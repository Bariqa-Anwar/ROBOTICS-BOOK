---
sidebar_position: 1
---

import { useColorMode } from '@docusaurus/theme-common';

<div style={{
  display: 'flex',
  flexDirection: 'column',
  alignItems: 'center',
  justifyContent: 'center',
  textAlign: 'center',
  padding: '4rem 2rem',
  backgroundColor: useColorMode().colorMode === 'dark' ? '#1a1a1a' : '#f5f5f7',
  borderRadius: '12px',
  border: `1px solid ${useColorMode().colorMode === 'dark' ? '#333' : '#ddd'}`
}}>
  <h1 style={{
    fontSize: '3.5rem',
    fontWeight: 'bold',
    color: useColorMode().colorMode === 'dark' ? '#fff' : '#000',
    marginBottom: '1rem'
  }}>
    Humanoid Robotics: A New Era
  </h1>
  <p style={{
    fontSize: '1.5rem',
    color: useColorMode().colorMode === 'dark' ? '#ccc' : '#555',
    marginBottom: '2rem',
    maxWidth: '800px'
  }}>
    An open-source book exploring the intersection of AI, simulation, and hardware to build the next generation of autonomous humanoid robots.
  </p>
  <a
    href="/docs/modules/01-ros2/00-overview"
    style={{
      backgroundColor: '#00ff9d',
      color: '#000',
      padding: '1rem 2.5rem',
      fontSize: '1.2rem',
      fontWeight: 'bold',
      borderRadius: '8px',
      textDecoration: 'none',
      transition: 'transform 0.2s ease, box-shadow 0.2s ease',
      boxShadow: '0 4px 15px rgba(0, 255, 157, 0.2)'
    }}
    onMouseOver={(e) => {
      e.currentTarget.style.transform = 'translateY(-3px)';
      e.currentTarget.style.boxShadow = '0 8px 25px rgba(0, 255, 157, 0.4)';
    }}
    onMouseOut={(e) => {
      e.currentTarget.style.transform = 'translateY(0)';
      e.currentTarget.style.boxShadow = '0 4px 15px rgba(0, 255, 157, 0.2)';
    }}
  >
    Start Learning
  </a>
</div>